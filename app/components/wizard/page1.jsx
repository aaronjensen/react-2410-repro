const React = require('react');

const ServiceWizardPage1 = React.createClass({
  render() {
    // const fields = FormBuilder.build(this.props.referral.pages.page1);

    return (
        <div>Check console for error</div>
    );
  }
});

module.exports = ServiceWizardPage1;
